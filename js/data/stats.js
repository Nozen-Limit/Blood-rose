/* ==========================================================================
   HERO STATS
   The three numbers under the headline on index.html.

     value  What to show. A number counts up on load; text appears as-is.
     label  The small caption beneath it.

   Add or remove entries freely — the strip lays out whatever it's given.
   ========================================================================== */

window.BR = window.BR || {};
window.BR.data = window.BR.data || {};

window.BR.data.stats = [
  /* ---------- Replace with your real numbers ---------- */
  { value: "2025", label: "Founded" },
  { value: "70+", label: "Daily Active Members" },
];
